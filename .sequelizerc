const {resolve} = require('node:path');
// 

module.exports = {
    'config': resolve(__dirname, 'src', 'config', 'database.js'), // esse config pega o caminho da nossa pasta (a raiz), procura por 'src' / 'config' / por ultimo database.js
    'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'),
    'models-path': resolve(__dirname, 'src', 'models')
}